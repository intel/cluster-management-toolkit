- hosts: selection:!controlplane:!nas
  vars:
    metadata:
      description: Reboot
      confirm: true
      allow-on-control-plane: never
      category: Administration
  gather_facts: false
  become: true
  become_user: root
  tasks:
  - name: Reboot system
    reboot:
