- hosts: selection
  vars:
    metadata:
      description: Execute command
      confirm: True
      query:
        string: Command to execute
        function: string
        variable: command
      category: Administration
  gather_facts: no
  tasks:
  - name: Execute command
    shell: "{{ command }}"
