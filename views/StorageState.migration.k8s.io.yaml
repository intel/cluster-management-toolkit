kind: "StorageState"
api_family: "migration.k8s.io"
default_command: "storagestates"
listview:
  name: "Storage States"
  group: "Provisioning"
  field_indexes:
    Normal:
      fields: ["name", "age"]
