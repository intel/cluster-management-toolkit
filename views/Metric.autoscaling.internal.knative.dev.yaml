kind: "Metric"
api_family: "autoscaling.internal.knative.dev"
default_command: "metric.autoscaling.internal.knative.dev"
command:
- "metrics.knative"
- "metric.knative"
listview:
  name: "Metric (KNative)"
  group: "KNative"
  field_indexes:
    Normal:
      # fields: ["namespace", "name", "age", "ready", "reason"]
      fields: ["namespace", "name", "age"]
