kind: "AccessKey"
api_family: "storage.loft.sh"
default_command: "accesskeys.storage"
command:
- "accesskey.storage"
listview:
  name: "Access Keys"
  group: "Lôft"
  field_indexes:
    Normal:
      fields: ["name", "age"]
